import React from 'react';
import {TextArea, TextAreaProps, Box} from '@adminjs/design-system'

const TextareaEdit: React.FC<TextAreaProps> = (props) => {
    return (<TextArea px={'500'} py={'200'}/>);
};

export default TextareaEdit;
